[project]
name = "scene-recall"
version = "0.1.0"
description = "Source-backed semantic search over film libraries"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.4.2",
    "fastapi>=0.138.1",
    "faster-whisper>=1.2.1",
    "filelock>=3.30,<4",
    "google-genai>=2.10.0",
    # Native FTS is experimental and its 0.33 index-maintenance contract is
    # covered by integration tests. Certify newer minor releases before use.
    "lancedb[embeddings]>=0.33,<0.34",
    "numpy>=1.26.0",
    "openai>=2.44.0",
    "open-clip-torch>=3.3,<4",
    "pillow>=10.0.0",
    "pyarrow>=14.0.0",
    "python-dotenv>=1.2.2",
    "python-multipart>=0.0.20",
    "pyyaml>=6.0.3",
    "torch>=2.11.0",
    "torchvision>=0.26.0",
    "timm>=1.0.27,<2",
    "transformers>=5.12.1",
    "transnetv2-pytorch>=1.0.5",
    "uvicorn[standard]>=0.49.0",
]

[dependency-groups]
dev = ["pytest>=9.1.1"]

[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true

[tool.uv.sources]
torch = [{index = "pytorch-cu128"}]
torchvision = [{index = "pytorch-cu128"}]
